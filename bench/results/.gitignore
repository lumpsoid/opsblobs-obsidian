# Transient "most recent run" pointers — regenerated by every `npm run bench`.
# The durable, committable artifacts are the date+profile-stamped files
# (e.g. 2026-07-23_xs-s-m.{json,md}), which a run can never silently clobber.
latest.json
latest.md
run.log
